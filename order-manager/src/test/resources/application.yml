datasources:
  default:
    url: jdbc:h2:mem:testDb
    driverClassName: org.h2.Driver
    dialect: H2
    username: sa
    password: ''
jpa:
  default:
    entity-scan:
      packages: 'com.thg.voyager.personalisation'

flyway:
  datasources:
    default:
      enabled: true
      locations:
        - filesystem:db/migration
